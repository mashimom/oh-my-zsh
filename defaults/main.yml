---
# defaults file for oh-my-zsh

ohmyzsh_users:
  - "{{ ansible_user_id }}"
  - "{{ ansible_env.SUDO_USER | default(ansible_user_id) }}"
ohmyzsh_plugins:
  - git
  - vagrant
  - docker
  - command-not-found
  - gradle
  - lein
ohmyzsh_theme: pygmalion
ohmyzsh_auto_update: "true"
